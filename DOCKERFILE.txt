
---

### 🐳 **2. `Dockerfile`** — для запуска в контейнере

> ⚠️ Tkinter не работает "из коробки" в Docker без графического вывода. Ниже — минимальная конфигурация для запуска с X11-пробросом (только Linux).

```dockerfile
# Используем легковесный образ Python
FROM python:3.11-slim

# Устанавливаем зависимости для GUI (Tkinter)
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        tk \
        tcl \
        x11-apps \
    && rm -rf /var/lib/apt/lists/*

# Устанавливаем переменную DISPLAY (для X11)
ENV DISPLAY=:0

# Копируем игру
COPY chess.txt /app/chess.py

# Устанавливаем рабочую директорию
WORKDIR /app

# Запуск
CMD ["python", "chess.py"]